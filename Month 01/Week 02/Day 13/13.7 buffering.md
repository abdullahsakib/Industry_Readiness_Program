
```python
with opne("big.txt","w") as f:
    for i in range(1_000_000):
        f.write("hello")


f.write() writes into ram not directly into disk

buffer flushes in large chunks

-when the buffer is full
-when the file is closed
-when the flush() is called

buffer is 

+much faster
+less disk waer
+fewer system calls
+better performance
